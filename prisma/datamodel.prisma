type ShoppingList {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
  items: [Item!]
}

type Item {
  id: ID! @unique
  name: String!
  quantity: Float
  price: [Float]
}
