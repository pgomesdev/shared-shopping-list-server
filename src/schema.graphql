type Query {
  info: String!
  listShoppingLists: [ShoppingList!]!
}

type Mutation {
  createShoppingList(
    name: String!
  ): ShoppingList!
}

type ShoppingList {
  id: ID!
  name: String!
  items: [String!]!
  store: [String!]!
}
