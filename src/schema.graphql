type Query {
  info: String!
  listShoppingLists: [ShoppingList!]!
}

type Mutation {
  createShoppingList(
    name: String!
  ): ShoppingList!
}

type ShoppingList {
  name: String!
  items: [String!]!
  store: [String!]!
}
