type Query {
  info: String!
  listShoppingList: [ShoppingList!]!
}

type Mutation {
  createShoppingList(
    name: String!
  ): ShoppingList!
}

type ShoppingList {
  name: String!
  items: [String!]!
  store: [String!]!
}
